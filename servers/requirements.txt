# pip install -r requirements.txt
# FastAPI 웹 프레임워크
fastapi~=0.123.0
uvicorn~=0.38.0

# AI
chromadb~=1.3.5

# 데이터 검증
pydantic~=2.12.5

# OpenAI API (LangChain 통합 버전)
langchain-openai~=0.3.0
langchain-community>=0.3.0,<0.4.0
langchain-chroma

# PDF Reader
# pypdf~=6.4.0 (PyMuPDFLoader 사용으로 대체)
pymupdf>=1.24.0

# 파일 업로드 처리
python-multipart~=0.0.20

# 환경 변수 관리
python-dotenv~=1.0.1

# 텍스트 분할 (LangChain)
langchain-text-splitters>=0.3.0,<0.4.0

langgraph>=0.2.0,<0.3.0
ragas>=0.2.0
rank_bm25>=0.2.2
rapidfuzz

# TourAPI HTTP client
httpx>=0.27.0,<1.0.0
pytest>=8.0,<9.0
